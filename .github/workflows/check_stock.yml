name: 🔍 Check RunPod Stock
on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install requests
        run: pip install requests
      - name: Check Stock
        env:
          RUNPOD_API_KEY: ${{ secrets.RUNPOD_API_KEY }}
        run: |
          python3 -c "
          import requests, os, json
          API_KEY = os.environ.get('RUNPOD_API_KEY')
          headers = {'Authorization': f'Bearer {API_KEY}', 'Content-Type': 'application/json'}
          query = '{ gpuTypes { id name secureStock communityStock } }'
          r = requests.post('https://api.runpod.io/graphql?api_key=' + API_KEY, json={'query': query}, headers=headers)
          print(f'Status: {r.status_code}')
          data = r.json()
          if 'data' in data:
              for g in data['data']['gpuTypes']:
                  if 'H100' in g['name'] or 'A100' in g['name']:
                      print(f\"ID: {g['id']}, Name: {g['name']}, Secure: {g['secureStock']}, Community: {g['communityStock']}\")
          else:
              print(f'Full Response: {json.dumps(data, indent=2)}')
          "
